apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "koperator.fullname" . }}-test-connection"
  labels:
    {{- include "koperator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "koperator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
