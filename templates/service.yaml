apiVersion: v1
kind: Service
metadata:
  name: {{ include "koperator-setup.fullname" . }}
  labels:
    {{- include "koperator-setup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "koperator-setup.selectorLabels" . | nindent 4 }}
