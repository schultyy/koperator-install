apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "koperator-setup.fullname" . }}-test-connection"
  labels:
    {{- include "koperator-setup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "koperator-setup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
